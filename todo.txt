	use constructor function for mock and actual
	constructor takes connection once, returns functions
incorrect use of conn.database, hardcode the table name (done?)
	escape the values that are being put into the database (can be done with ('?', '?'))
	use "?"s as placeholders
	INSERT INTO table (hash, col1, col2) VALUES ('?', '?', '?')

a USE query can be used (see what i did there?) to connect to a certain database

function Whatever(conn) {
	return {
		insert: insert.bind(null, conn),
		get: get.bind(null, conn)
	}
}

id
hash
volume
parts
dims

find js module to get md5 from stream, and stream a file into it.

write readme.md for this module